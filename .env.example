# === Telegram ===
BOT_TOKEN=your-telegram-bot-token
ALLOWED_USERS=your-telegram-user-id

# === Agent ===
WORKING_DIR=~
MAX_BUDGET_USD=5.0

# === Model Provider ===
# Primary: claude-sdk | gpt-4o | gpt-4o-mini | gemini-2.5-pro | gemini-2.5-flash | nvidia-llama-3.3-70b | ollama | openrouter
PRIMARY_PROVIDER=claude-sdk
# Fallback chain (comma-separated, tried in order if primary fails)
FALLBACK_PROVIDERS=

# === API Keys (set the ones you need) ===
# Claude SDK: No key needed — uses `claude login` (Max subscription)
# Voice transcription (Groq Whisper — free at console.groq.com)
GROQ_API_KEY=
# OpenAI (for GPT-4o, GPT-4o-mini)
OPENAI_API_KEY=
# Google (for Gemini models)
GOOGLE_API_KEY=
# NVIDIA NIM (free tier — 150+ models at build.nvidia.com)
NVIDIA_API_KEY=
# OpenRouter (any model, one API — openrouter.ai)
OPENROUTER_API_KEY=
